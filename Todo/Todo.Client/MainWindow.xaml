<Window x:Class="Todo.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="2*" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Grid.Row="2" Content="Add new" FontSize="16" Margin="10 10 5 10" />
        <Button Grid.Row="2" Grid.Column="1" Content="Delete" FontSize="16" Margin="5 10 10 10" />
        <DataGrid Grid.ColumnSpan="2" Margin="10" ItemsSource="{Binding Todos}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Description}" />
                <DataGridTextColumn Binding="{Binding StartDate}" />
                <DataGridTextColumn Binding="{Binding EndDate}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>

